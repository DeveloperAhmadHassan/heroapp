class WorkoutData {
  final String title;
  final int durationMinStart;
  final int durationMinEnd;

  const WorkoutData({
    required this.title,
    required this.durationMinStart,
    required this.durationMinEnd,
  });
}